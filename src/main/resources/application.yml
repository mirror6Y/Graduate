#根据不同环境选用不同的配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springmvc?serverTimezone=GMT%2B8&amp;useSSL=false
    username: root
    password: mirror6